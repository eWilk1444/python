# Calculates area of a rectangle with the formula: area = length * width

# Dimensions of rectangle inputs
length = 12
width = 7

# Calculates area of rectangle based on inputs
area = length * width

# Prints calculation to console
print("\nThe area of the rectangle with a length of", str(
    length), "and a width of", str(width), "is", str(area), "\n")
